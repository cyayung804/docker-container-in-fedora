Alpine Linux Docker images

Alpine Linux Docker images published to Docker Hub.

A GitHub Actions workflow runs daily to fetch the latest tags for each component and language. If a version file changes, a CI/CD build is triggered automatically for the images in src/.
